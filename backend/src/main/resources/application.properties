spring.application.name=poppin

spring.datasource.url=${MYSQL_URL}
spring.datasource.username=root
spring.datasource.password=${MYSQL_ROOT_PASSWORD}

# redis setting
spring.data.redis.host=redis
spring.data.redis.port=6379

spring.output.ansi.enabled=always

spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.format-sql=true

logging.level.org.hibernate.SQL=debug
logging.level.org.hibernate.sql=trace

spring.jwt.secret=${JWT_SECRET_KEY}

snowflake.datacenter-id=1
snowflake.worker-id=1

spring.social.naverId=${NAVER_ID}
spring.social.naverSecret=${NAVER_SECRET}
spring.social.kakaoId=${KAKAO_ID}

# mongodb
spring.data.mongodb.uri=mongodb://127.0.0.1:27017/matching
spring.data.mongodb.username=${MONGO_INITDB_ROOT_USERNAME}
spring.data.mongodb.password=${MONGO_INITDB_ROOT_PASSWORD}
spring.data.mongodb.test.connectionString=mongodb://localhost:27017/test

#?????
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=100MB
file.upload-dir=./uploads

